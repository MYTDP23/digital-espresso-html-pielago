This website is structures using semantic HTML5 elements to ensure accessibility and SEO optimization. All pages are connected through a global navigation menu using relative links. Images are stored in a dedicated "images" folder for clean file organization. The site follows a hierarchical document outline using header, main, section, article, aside and footer elements.

